---
import Book from "../Book.astro";

const background = ["bkg-light", "bkg-peps", "bkg-light"];

const tarifs = [
  {
    duration: "1 mois",
    title: "1 sortie",
    text: "Lance-toi avec un programme structuré et un accompagnement concret dès la première semaine.",
    price: "89",
    nb: "1",
  },
  {
    duration: "2 mois",
    title: "3 sorties",
    text: "Progresse avec un suivi qui s'adapte à toi et à tes résultats.",
    price: "219",
    nb: "3",
  },
  {
    duration: "3 mois",
    title: "5 sorties",
    text: "Vise un objectif précis et mets toutes les chances de ton côté sur la durée.",
    price: "349",
    nb: "5",
  },
];
---

<div id="container">
  <main id="content" class="d-flex-column bkg-dark">
    <!-- BLOC 1 : A la carte-->
    <section
      aria-labelledby="Alacarte-title"
      class="d-flex-column align-items-center gap2"
    >
      <h1 id="tarifs-title" class="text-color-peps">
        Séances privées supplémentaires
      </h1>
      <span id="blocs" class="d-flex gap2 text-color-light">
        {
          tarifs.map((item, index) => (
            <article id="article" class="d-flex-column bloc gap2">
              <p
                id="duration"
                class={`px1 border-radius-1 ${background[index]} text-color-dark`}
              >
                {item.duration}
              </p>
              <h2>{item.title}</h2>
              <p id="text">{item.text}</p>
              <section class="d-flex gapx">
                <h2>{item.price}</h2>
                <p>CHF</p>
              </section>
              <hr class="separator" />
              <section>
                <section>
                  <ul>
                    <li>Séance à la carte pour 1 à 2 personnes</li>
                    <li>
                      {`${item.nb} entraînement${item.nb > 1 ? "s" : ""} privatif${
                        item.nb > 1 ? "s" : ""
                      } accompagné${item.nb > 1 ? "s" : ""}`}
                    </li>
                    <li>Exercices personnalisés</li>
                    <li>Supervision et correction des mouvements</li>
                  </ul>
                </section>
              </section>
            </article>
          ))
        }
      </span>
      <Book color="light" style="margin-top: auto;" />
    </section>
  </main>
</div>

<style>
  #content {
    padding: 6rem;
  }

  #article {
    width: 420px;
  }

  #duration {
    margin-bottom: 2rem;
    font-size: 12px;
    max-width: fit-content;
  }

  #text {
    height: 80px;
  }

  h2 {
    margin-top: -0.8rem;
  }

  li {
    margin: 0 2rem;
    list-style: circle;
  }

  /* =========================
     GRAND ECRAN
  ========================= */
  @media screen and (max-width: 1440px) {
    #content {
      padding: 3rem;
    }

    #article {
      width: 100%;
      max-width: 560px;
    }

    #text {
      height: auto;
    }
  }

  /* =========================
     TABLETTE
  ========================= */
  @media screen and (max-width: 1024px) {
    #content {
      padding: 3rem;
    }

    #blocs {
      flex-direction: column;
      align-items: center;
    }

    #article {
      width: 100%;
      max-width: 560px;
    }

    #text {
      height: auto;
    }
  }

  /* =========================
     MOBILE
  ========================= */
  @media screen and (max-width: 768px) {
    #content {
      padding: 2rem 1.5rem;
    }

    h1 {
      font-size: 32px;
    }

    #article {
      width: 100%;
      max-width: 100%;
    }
  }

  /* =========================
     PETITS SMARTPHONES
  ========================= */
  @media screen and (max-width: 480px) {
    #content {
      padding: 1.5rem 1rem;
    }

    h1 {
      font-size: 18px;
    }

    li {
      margin: 0 0.5rem;
    }
  }
</style>
